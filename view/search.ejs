<h1>搜索<%= search%>的结果：</h1>
<% if(results.length) {%>
    <ul>
        <% for (var i=0; i<results.length; i++){ %>
        <li><%= result[i].text %> - <em><%= results[i].from_user %></em>></li>
    </ul>
<% }else{ %>
    <p>No results</p>
<% } %>